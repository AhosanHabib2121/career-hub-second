import Navbar from "../navbar/Navbar";

const Header = () => {
    return (
        <div>
            {/* Navbar area */}
            <Navbar></Navbar>
        </div>
    );
};

export default Header;